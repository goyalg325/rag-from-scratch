# FastAPI Backend Design

FastAPI is a good fit for RAG services because it provides validation, async support, and a simple REST interface.

This project exposes endpoints for health checks, querying, and rebuilding the document index.
